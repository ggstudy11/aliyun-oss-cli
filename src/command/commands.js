const list = require('./list');
const upload = require('./upload');
const help = require('./help');
const clear = require('./clear');
const exit = require('./exit');

module.exports = { list, upload, help, clear, exit };
